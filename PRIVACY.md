# Privacy

Best Harness runs locally. The Go CLI reads Git metadata and named repository files only when invoked. It does not send repository data, task records, credentials, or agent-session content to a service.

The optional Codex Skill follows the permissions and tool policy of the active Codex session. Users choose whether to run commands that read or write local files.
